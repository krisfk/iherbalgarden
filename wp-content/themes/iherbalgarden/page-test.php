<?php

echo 'test email';
// wp_mail( $to, $subject, $message, $headers, $attachments );
wp_mail( 'krisfk@gmail.com', 'subject', 'msg' );

                // date_default_timezone_set('Asia/Hong_Kong');

                // echo date('d/m/Y');
                // $args = array(
                //     'post_type' => 'school',    
                //     'posts_per_page' => -1       
                // );
        
                //    $query = new WP_Query( $args );
        
                //    if ( $query->have_posts() ) {   

                
                //     while ( $query->have_posts() ) {
                
                //        $query->the_post();
                //     //    print_r(get_field('school_account'));
                       
                //     // if(get_field('school_account')['user_nicename']==);
                //     //    echo '<br>';

                //     }
                // }
?>